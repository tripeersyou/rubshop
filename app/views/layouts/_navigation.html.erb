<% if (!supplier_signed_in? && !seller_signed_in? && !buyer_signed_in?) %>
<ul id="dropdown1" class="dropdown-content">
  <li><a href="/suppliers/sign_in">Supplier</a></li>
  <li><a href="/buyers/sign_in">Buyer</a></li>
  <li><a href="/sellers/sign_in">Company</a></li>
</ul>
<% end %>
<nav class="nav-extended">
  <div class="nav-wrapper">
    <div class="container">
      <a href="#" class="brand-logo">Logo</a>
      <a href="#" data-activates="mobile-demo" class="button-collapse"><i class="material-icons">menu</i></a>
      <ul id="nav-mobile" class="right hide-on-med-and-down">
        <% if (!supplier_signed_in? && !seller_signed_in? && !buyer_signed_in?) %>
         <li><a class="dropdown-button" href="#!" data-activates="dropdown1">Log in as<i class="material-icons right">arrow_drop_down</i></a></li>
        <% else %>
          <% if supplier_signed_in? %>
            <li><a><%= link_to 'New Posting', new_posting_path %></a></li>
            <li><%= link_to 'Sign Out', destroy_supplier_session_path, method: :delete %></li>
          <% elsif buyer_signed_in?%>
            <li><%= link_to 'Sign Out', destroy_buyer_session_path, method: :delete %></li>
          <% elsif seller_signed_in? %>
           <li><a><%= link_to 'New Product', new_product_path %></a></li>
            <li><%= link_to 'Sign Out', destroy_seller_session_path, method: :delete %></li>
          <% end %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
